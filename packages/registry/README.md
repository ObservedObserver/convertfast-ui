# ConvertFast Registry Workspace

This workspace stores the canonical block manifest for generating a shadcn-compatible open source registry.

Planned outputs:

1. `registry.json`
2. `<item-name>.json` for each item

The manifest file lives at:

- `/Users/observedobserver/Documents/GitHub/convertfast-ui/packages/registry/manifest/blocks.json`

Initial scope includes default and editorial variants for the current landing-page sections.

Commands:

```bash
yarn registry:build
yarn registry:validate
```
