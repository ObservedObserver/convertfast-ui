# Segments Preview

Run the preview app:

```bash
yarn workspace segments dev
```

Template preview options in dev:

- default template: `http://localhost:5173/`
- editorial template: `http://localhost:5173/?template=editorial`

You can also switch templates from the sticky toolbar in the preview UI.
